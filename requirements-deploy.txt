# requirements-deploy.txt — dependances du DEPLOIEMENT (niveau 1 uniquement)
#
# Ce fichier a ete SCINDE en 4 (2026-09-18) pour corriger un OOMKilled recurrent
# sur la tache "build-image" du pipeline Tekton : un seul gros pip install fait
# calculer a pip TOUT le graphe de dependances (chromadb, unstructured/spacy,
# toute la famille langchain...) en une seule fois, ce qui depasse la limite
# memoire par defaut de cette tache. Voir le Dockerfile (RUN pip install
# successifs) et les fichiers ci-dessous, chacun installe separement pour reduire
# le pic memoire du resolveur a chaque etape :
#   - requirements-deploy-1-core.txt        (streamlit, config, calcul)
#   - requirements-deploy-2-langchain.txt   (famille LangChain)
#   - requirements-deploy-3-vectorstore.txt (chromadb, sentence-transformers)
#   - requirements-deploy-4-documents.txt   (extraction documents, scikit-learn, anthropic)
#
# requirements.txt (a la racine, non utilise ici) reste un dump complet du venv
# de l'exercice, niveau 1+2+3 melanges - non pertinent pour ce deploiement.
#
# torch : PAS de version ici avec "+cu128" (build CUDA, plusieurs Go, inutile sans
# GPU) - installe separement dans le Dockerfile depuis l'index CPU officiel
# PyTorch (voir Dockerfile).
